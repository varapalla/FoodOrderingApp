{
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "scripts": {
    "build": "ng lint && ng test --code-coverage --watch=false && ng build --configuration production"
  }
}
